const findKeyByValue = function (start, finish) {
    Object.keys(start).forEach(key => {
    result = (start[key]);
    result2 = (key)
    if (result === finish) {
    return result2
  } else {
      result2 = undefined
  }});
  console.log(result2)
  return result2
};

module.exports = findKeyByValue


